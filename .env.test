# Test Environment Configuration
# Used by E2E and integration tests

# Database configuration (absolute path for test reliability)
DATABASE_URL=file:/home/openhoat/work/mesame-conversation-history/prisma/test.db

# Use mock provider to avoid real API calls
MESAME_PROVIDER=mock
MESAME_MODEL=mock-model

# Server configuration for tests
MESAME_PORT=0
MESAME_LOG_LEVEL=silent
