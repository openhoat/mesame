# Continuous Integration Pipeline
# Optimized: single setup job, cached dependencies, build artifacts
name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # Job 1: Setup - Install dependencies once and cache for all other jobs
  setup:
    name: Setup Dependencies
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22.x'

      - name: Cache node modules
        uses: actions/cache@v5
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Cache Wireit
        uses: actions/cache@v5
        with:
          path: .wireit
          key: ${{ runner.os }}-wireit-${{ hashFiles('package-lock.json', 'src/**/*.ts', 'electron/**/*.ts') }}
          restore-keys: |
            ${{ runner.os }}-wireit-

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npm run db:generate

      - name: Upload node_modules
        uses: actions/upload-artifact@v4
        with:
          name: node-modules
          path: node_modules
          retention-days: 1

      - name: Upload .wireit cache
        uses: actions/upload-artifact@v4
        with:
          name: wireit-cache
          path: .wireit
          retention-days: 1

      - name: Upload Prisma client
        uses: actions/upload-artifact@v4
        with:
          name: prisma-client
          path: node_modules/.prisma
          retention-days: 1

  # Job 2: Validate - Lint, typecheck, test with coverage
  validate:
    name: Validate
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22.x'

      - name: Download node_modules
        uses: actions/download-artifact@v4
        with:
          name: node-modules
          path: node_modules

      - name: Download Prisma client
        uses: actions/download-artifact@v4
        with:
          name: prisma-client
          path: node_modules/.prisma

      - name: Validate (lint + typecheck + test with coverage)
        run: npm run validate

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/lcov.info
          fail_ci_if_error: false

  # Job 3: Build - Compile TypeScript and Vite builds
  build:
    name: Build
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22.x'

      - name: Download node_modules
        uses: actions/download-artifact@v4
        with:
          name: node-modules
          path: node_modules

      - name: Download Wireit cache
        uses: actions/download-artifact@v4
        with:
          name: wireit-cache
          path: .wireit

      - name: Download Prisma client
        uses: actions/download-artifact@v4
        with:
          name: prisma-client
          path: node_modules/.prisma

      - name: Build all
        run: npm run build:all

      - name: Upload dist artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-artifacts
          path: |
            dist
            dist-electron
            electron/renderer/dist
          retention-days: 1

  # Job 4: E2E Tests - Run Playwright tests with pre-built artifacts
  e2e-tests:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: [validate, build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22.x'

      - name: Download node_modules
        uses: actions/download-artifact@v4
        with:
          name: node-modules
          path: node_modules

      - name: Download Prisma client
        uses: actions/download-artifact@v4
        with:
          name: prisma-client
          path: node_modules/.prisma

      - name: Download dist artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist-artifacts
          path: .

      - name: Cache Playwright browsers
        uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Playwright system dependencies
        run: npx playwright install-deps

      - name: Initialize database
        run: npm run db:push

      - name: Run E2E tests
        run: npm run test:e2e:headless
        env:
          HEADLESS: true
          MESAME_LOG_LEVEL: silent
          CI: true

      - name: Upload E2E test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-test-results
          path: |
            e2e-report/
            test-results/
          retention-days: 7